---
notification: minor
notification-js: minor
---

Emit `actionPerformed` events on desktop when a notification is clicked or dismissed, and implement `window.Notification` parity with `onclick`, `onclose` and `close()` on desktop.
